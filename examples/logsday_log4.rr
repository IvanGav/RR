log4 = ["Hello", "everyone", "it's", "logsday", "log", 4]
print( concat(log4, " ") + "!" )

"I've done a few things, such as:"

block_returns = {
    blocks = 222
    blocks = blocks*2
    blocks
}

function_calls = max(block_returns, 1000)

ifs = if (function_calls == 444) { function_calls*2 } else { function_calls*10 }

lists = [block_returns, "Logsday", ifs]

print("lists should have '444', 'Logsday' and '10000'")
print(lists)

"See you next Logsday!"